---
services:
  # A ReactJS web service
  - type: web
    name: reactjsexample
    env: docker
    region: frankfurt # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    rootDir: Docker/reactjs
    autoDeploy: false # optional
 
   # A Vue JS web service
  - type: web
    name: vuejsexample
    env: docker
    region: frankfurt # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    rootDir: Docker/vuejs
    autoDeploy: false # optional

   # A Go web service
  - type: web
    name: goexample
    env: docker
    region: frankfurt # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    rootDir: Docker/go
    autoDeploy: false # optional

   # A Flask web service
  - type: web
    name: flaskexample
    env: docker
    region: frankfurt # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    rootDir: Docker/flask
    autoDeploy: false # optional

   # A Nginx web service
  - type: web
    name: nginxexample
    env: docker
    region: frankfurt # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    rootDir: Docker/nginx
    autoDeploy: false # optional

   # A Apache web service
  - type: web
    name: apacheexample
    env: docker
    region: frankfurt # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    rootDir: Docker/apache
    autoDeploy: false # optional

   # A Ember JS web service
  - type: web
    name: emberjsexample
    env: docker
    region: frankfurt # optional (defaults to oregon)
    plan: free # optional (defaults to starter)
    rootDir: Docker/emberjs
    autoDeploy: false # optional
